name: Commit File Index

on:
  push:
    branches:
      - main
      - master
    paths:
      - 'docs/references/**'
      - '.github/workflows/push.yml'

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.x'

    - name: Generate PDF list (Node.js)
      run: node docs/references/generate-pdf-index.js

    - name: Generate TXT list (Python)
      run: python docs/references/generate_txt_index.py

    - name: Commit and push changes
      uses: stefanzweifel/git-auto-commit-action@v5
      with:
        commit_message: "chore: update file registries (PDF & TXT)"
        file_pattern: "docs/references/files.json docs/references/txt_files.json"
